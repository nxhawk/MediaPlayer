<Window x:Class="MediaPlayer.Views.Dialog.NewPlaylist"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaPlayer.Views.Dialog"
        mc:Ignorable="d"
        Background="#2a213a"
        FontFamily="Inter"
        Title="Media Player Dialog" Height="150" Width="300"
        FocusManager.FocusedElement="{x:Reference playlistName}"
        >
    <StackPanel Orientation="Vertical" VerticalAlignment="Center">
        <TextBox x:Name="playlistName" Width="250" Height="40"
                 FontSize="20"
                 KeyDown="OnKeyDownHandler"
                 Foreground="#fff"
                 Margin="10"/>
        <Button
            Width="130"
            Click="Button_Click"
            Background="#9b4de0"
            Content="Add new"
            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
            Style="{StaticResource MaterialDesignRaisedSecondaryDarkButton}"
            ToolTip="Resource name: MaterialDesignRaisedSecondaryDarkButton" 
        />
    </StackPanel>
</Window>
